{{#with data.products}}
{{#if isEmpty}}
<div>
    <mark class="list_mark">Пустий</mark>
</div>
{{else}}
{{#each list}}
<div class="product__card">
    <div class="product__name">{{name}}</div>
    <div class="product__description">{{description}}</div>
    <div class="product__id">ID: {{id}}</div>
    <div class="product__row">
        <div class="product__price">{{price}}$</div>
        <button class="product__edit"><a href="/product-edit?id={{id}}"
                class="product__edit_button">Редагувати</a></button>
    </div>
</div>
{{/each}}
{{/if}}
{{/with}}